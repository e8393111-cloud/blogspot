# 자동화 스모크 테스트 — 2026-09-09 (KST)

트리거 `trig_01SYQNKfEu1ANkw7qbLhYecA` · 발사 22:52:14Z · 종료 22:56:18Z (**4분**)
발사된 세션 `session_01MyWQLQY4BV1wcHVH4KyLTk` · 상태 IDLE · 비용 $0.56

## 결과 — **FAIL. 자동 실행을 걸면 안 된다.**

| 점검 | 결과 | 근거 |
|---|---|---|
| 0. 브랜치 정렬 | **FAIL** | 세션에 **저장소가 아예 없다** |
| 1. 저장소 문서 읽기 | **FAIL** | 위와 동일 |
| 2. ★Make MCP | **FAIL** | 트리거 생성 시 시스템이 경고했고, 트리거 config의 `mcp_servers: []` |
| 3. Agent 서브에이전트 | **미실행** | 앞 단계에서 막혀 도달 못 함 |
| 4. 커밋·푸시 | **FAIL** | 커밋 0건. 원격에 `.claude/auto/` 없음 |

이 파일은 **테스트 세션이 아니라 메인 세션이 대신 기록**한 것이다.

## 왜 실패했나 — 3가지가 동시에 빠졌다

1. **저장소가 안 붙는다.** 트리거의 `session_request.config.sources`가 `[]`이고,
   발사된 세션의 `session_context`에도 `sources`가 없다. `git fetch`할 대상 자체가 없다.
2. **MCP 커넥터가 안 붙는다.** 트리거 생성 시 시스템 경고 그대로다 —
   *"이 트리거는 커넥터를 저장하지 않으므로 발사된 세션에 `mcp__<server>__*` 도구가 없다"*.
   Make가 계정에 연결돼 있어도 **트리거가 넘겨주지 못한다.**
3. **모델이 다르다.** `last_served_model: claude-sonnet-5`.
   메인 세션은 Opus인데 예약 세션은 Sonnet으로 떴다. 무인 집필·평가 품질에 영향이 있다.

## ★이게 GlowShot 7연속 실패의 정체다

`trig_01A2FkkHzh57Udfn5NHgocB6`(GlowShot 콘텐츠 사이클)이 **새 세션 방식으로 7번 연속 실패**하고
2026-08-24에 비활성화됐다. 당시 원인을 "npm install 누락 + 브랜치 뒤처짐"으로 적어놨는데,
**진짜 원인은 그게 아니라 저장소가 아예 안 붙는 것**이었다. 같은 함정에 두 번 빠질 뻔했다.

지금 계정에서 **실제로 도는 Routine은 전부 `persist_session: true`**로
살아 있는 세션에 물려 있다(GlowShot 재구성판·아침 글감 브리핑·AdSense 점검).
그게 이 계정에서 검증된 유일한 방식이다.

## 다음 수

`create_new_session_on_fire: true` 방식은 **폐기**한다. 대안 3가지:

1. **상주 세션에 물리기** — 검증된 방식. 단 세션 컨텍스트가 계속 쌓인다
   (메인 세션은 이미 524,702/1,000,000 = 52% 사용).
2. **전용 세션을 만들어 물리기** — `create_session`에 `source_url`을 주면 저장소는 붙는다.
   **Make MCP가 붙는지는 미확인** — 이것부터 확인해야 한다.
3. **claude.ai 루틴 UI에서 은희님이 직접 생성** — 시스템 경고가 안내한 방법.
   웹에서 만들면 커넥터를 붙일 수 있다.

## 부수 확인
- **main 오염 없음.** `origin/main` 최신 2건 모두 Make의 `chore: update posts.json`. 정상.
