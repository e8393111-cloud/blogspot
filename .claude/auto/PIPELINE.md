# 자동 발행 파이프라인 — 하루 3회 실행 절차

> 이 문서가 Routine의 **실행 지시서 본체**다. 트리거 프롬프트는 "이 파일을 읽고 따르라"만 말한다.
> 절차를 바꾸려면 트리거가 아니라 **이 파일을 고친다.**
> 대상 세션: `session_01Ntjy5x2ZiuamxQwP234PEm` (🤖 여행블로그 자동 파이프라인)
> 발사: `0 23,7,15 * * *` UTC = **KST 08:00 / 16:00 / 00:00**

---

## ★이 환경에서 확인된 함정 4가지 (2026-09-09 스모크 + 실행 1회차 결과)

1. **★`run_in_background: false`는 먹히지 않는다. 서브에이전트는 무조건 백그라운드로 뜬다.**
   2026-09-09 실행에서 4번 모두 `false`로 호출했는데 4번 다 `Async agent launched`가 돌아왔다.
   → **"포그라운드로 호출한다"는 예전 대책은 폐기한다.** 하네스가 무시하므로 지킬 수가 없다.
   → **대신 이렇게 한다 (실제로 작동한 방법):**
   　 ① 에이전트를 띄우기 **직전에 `send_later`로 자기 체크인을 건다**(취재·집필·평가는 10~15분, 긴 건 50분).
   　 　 `send_later`는 이 세션으로 **확실히 배달된다** — 2026-09-09에 4/4 성공했다. 이게 유일하게 믿을 수 있는 안전줄이다.
   　 ② 완료 알림이 오면 그대로 진행한다(같은 날 4/4 정상 도착했다. **다만 전날엔 0/1이었으니 믿고 놀지 마라**).
   　 ③ 알림이 안 오면 **트랜스크립트 메타데이터만** 본다(전문을 tail 하면 컨텍스트가 터진다):
   ```
   F=/root/.claude/projects/-home-user-blogspot/<세션UUID>/subagents/agent-<agentId>.jsonl
   ls -la "$F"        # mtime이 계속 갱신되면 아직 작업 중
   wc -l "$F"         # 이벤트 수가 늘고 있으면 살아 있음
   ```
   　 mtime이 5분 이상 멈췄고 마지막 assistant 이벤트의 `stop_reason`이 `end_turn`이면 **끝난 것**이다.
   　 그 마지막 메시지의 text 블록만 python으로 파싱해 결과를 꺼내 쓴다(전날 실제로 그렇게 살려냈다).
2. **Make 도구 프리픽스가 MCP 재연결마다 바뀐다.** 같은 세션 안에서 실제로 바뀌는 걸 확인했다 —
   처음엔 `mcp__dd5a2de6-0438-43ff-b141-005d5c400e62__*`(UUID)로 붙었다가,
   서버가 재연결된 뒤엔 `mcp__Make__*`로 붙었다. 둘 다 같은 시나리오 6177668을 정상 조회한다.
   → **어느 쪽도 정답으로 외우지 마라.** 이름을 쓰기 전에 매번 **`ToolSearch`에
   `scenarios_run` `data-store-records_list` 같은 기능어로 검색**해 그 시점의 실제 이름을 찾아 쓴다.
   `select:` 로 이름을 찍어 넣었다가 `No matching deferred tools found`가 나오면
   **Make가 죽은 게 아니라 프리픽스가 바뀐 것이다** — 기능어 검색으로 다시 찾으면 된다.
3. **예약 세션(`create_new_session_on_fire`)은 저장소도 MCP도 안 붙는다.**
   → 이 파이프라인을 **새 세션 방식으로 옮기지 마라.** 상주 세션 전용이다.
   (GlowShot이 이걸 몰라 7연속 실패했다 — `SMOKE-2026-09-09.md`)
   ★보강 근거: `create_trigger` 응답이 **"this trigger stores no MCP connectors, so the sessions it fires
   will run without connector (mcp__*) tools"** 라고 직접 경고한다. 새 세션엔 Make도 GitHub도 없다.

4. **★`fire_trigger`(강제 실행)만 새 세션으로 샌다. 예약 발사는 멀쩡하다.** — 2026-09-09 실측
   - `fire_trigger`로 파이프라인 루틴을 쐈더니 이 세션이 아니라 **새 세션**이 떴다
     (`origin: force_run_trigger`, sonnet-5, **80초 만에 idle**, 커밋 0건). MCP가 없으니 당연하다.
   - 그래서 "예약도 새 세션으로 새는 것 아니냐"고 의심했으나 **틀린 추측이었다.**
     파이프라인 루틴과 똑같은 모양(`persistent_session_id` 명시 + `run_once_at`)으로 검증 트리거를
     쐈더니 **이 세션으로 정확히 배달됐다**(`trig_019i6NbJgYqc865HZ7sxyuMU`, 01:00:17Z 발사 → 도착).
     `send_later`도 같은 날 **4/4 전부 이 세션 도착**했다.
   → **결론: cron 정규 발사는 정상 작동한다. 루틴을 손댈 필요 없다.**
   → ★**단 루틴을 수동으로 시험할 때 `fire_trigger`를 쓰지 마라.** 빈 새 세션이 떠서
     "파이프라인이 고장 났다"는 잘못된 결론을 낳는다(실제로 그렇게 오진했다).
     수동 시험은 **이 세션에서 이 문서를 직접 따라 실행**하거나, `run_once_at`을 몇 분 뒤로 건 일회성 트리거로 한다.

---

## 0. 시작 전

```
git fetch origin claude/blog-agents-seniors-bdb75n
git checkout -B claude/blog-agents-seniors-bdb75n origin/claude/blog-agents-seniors-bdb75n
```
★**main 체크아웃 금지 · main 푸시 금지 · PR 생성 금지.**
이 브랜치의 `posts.json`은 main보다 뒤처져 있다. 섞이면 라이브 사이트 최근글이 한 달 되돌아간다.

**긴 작업 폴백**: 시작할 때 `send_later`로 50분 뒤 자기 체크인을 걸어 둔다.
멈추면 그 알람이 세션을 깨운다(스모크에서 실제로 그렇게 살아났다).

## 0-1. 성과와 기존 글 보강부터 확인

`.claude/performance/README.md`와 `recovery-queue.json`을 읽는다.
핵심 질문·제목·실제 발행 상태 기준은 이 개정을 적용한다. 긴급 사실 정정, 미완료 보강, 측정 기한 도래를 확인한다.
보강을 수행한 실행은 RUNLOG에 `기존글 보강`으로 기록하고 새 원고 생산과 구분한다. 새 예약 트리거를 만든 것으로 설명하지 않는다.
점검/보강이 끝났거나 현재 접근으로 진행할 수 없으면 사유를 기록하고 다음 단계를 수행한다.

## 1. 오늘 몫이 남았는지

`.claude/auto/RUNLOG.md`를 읽는다.
- 오늘(KST) 기록이 **3건이면 아무것도 하지 않고 종료**한다.
- 직전 실행이 원고를 커밋하지 못하고 끝났으면 **새 주제를 시작하지 말고 그것부터 끝낸다.**

## 2. 주제 선택 — `.claude/travel-calendar/calendar.tsv`

```
상태 = 미발행
AND 발행권장이 오늘부터 3주 이내
AND 경쟁률 < 5
AND 뚜벅이 ≠ 불가
정렬: 발행권장 임박 순 → 월검색량 큰 순
```
- `.claude/blog-topic-pipeline.md`의 기존 글과 실제 공개 URL·내용을 대조한다. 같은 지역의 다른 시즌/질문을 이미 발행한 것으로 간주하지 않는다.
- 위 검색량/경쟁률 정렬은 후보 추출용이다. 최종 선택은 실제 유입·질문 적합성·취재 가능성을 비교해 NOTES에 근거를 남긴다.
- 조건을 만족하는 게 **없으면** 글을 쓰지 말고 **키워드 실측만** 하고 종료
  (`.claude/keyword-tool.md`, Make 6031917 → 6032112). 캘린더에 새 지역을 채운다.

## 3. 취재 — `travel-journalist` (★띄우기 직전 `send_later` 15분 체크인)

프롬프트에 반드시 넣을 것:
- **뚜벅이 가능 판정을 가장 먼저** (불가면 그 주제를 접고 캘린더 `뚜벅이=불가`로 갱신)
- **폐장 / 매표마감 / 입장마감 / 탈것 운행종료 — 네 값을 각각 다른 출처 문장에서 따로**
- 출발지를 서울로 고정하지 말 것. 목적지 근거리 대도시 병기
- 모든 숫자에 **등급(A=공식 직접열람 / B=교차2건 / C=2차 / D=추정)**
- ★**C등급으로 시각·요금·버스번호를 단정하지 말 것**
- 마지막 1구간(정류장→목적지, 하차 후 도보 포함)은 **"운영자 네이버지도 확인 필요"**로 표시

## 4. 공식값 보강 — `.claude/web-tool.md`

기자가 C등급으로 남긴 **요금·운영시간·마감시각**을 지자체 공식 페이지에서 A등급으로 올린다.
연천에서 시티투어 시간표·요금·버스 배차를 전부 이 경로로 뚫었다.
★기자 값과 다르면 **공식값을 쓴다.** (연천 재인폭포 요금이 실제로 달랐다)

## 5. 집필

`naver-senior-travel-blogger` 호출(★띄우기 직전 `send_later` 15분 체크인). `CLAUDE.md` 본문 규칙 전부 적용.
★**에이전트가 응답하지 않거나 결과가 부실하면 세션이 직접 쓴다.** 기다리다 세션을 낭비하지 않는다.

커밋 전 강조 개수를 세어 맞춘다:
```
[굵게] 8~12 · [색변경] 4~7 · [박스] 3~5 · [소제목] 4~7 · [버튼링크] 2+ · 마크다운 표 0 · '확인'류 ≤3
```

## 6. 평가 — `reader-panel-evaluator` (★띄우기 직전 `send_later` 10분 체크인), 최대 4라운드

- **130 이상 AND 치명결함 0** → `상태: 통과`
- 4라운드에도 미달 → ★**가짜 통과 금지.** `상태: 미달(최고점 N)`으로 정직히 적고 **그대로 커밋**한다.
- ★**평가단 지적도 근거를 확인하고 반영한다.** 틀린 지적이 실제로 나온다
  (연천에서 "전곡역에서 걸어가긴 멀다"는 제안이 사실과 반대였다).

## 7. 사진 — `.claude/photo-tool.md`

TourAPI 조회 후 `cpyrhtDivCd`를 **사진 단위로** 확인해 Type1(오버레이 가능)/Type3(변경금지) 분리 기록.
★사진을 열어 볼 수 없으면 **`육안 미확인`**으로 적는다. **열어본 척하지 않는다.**

## 8. 커밋 — `.claude/posts/<발행예정일>-<주키워드>/`

```
area1.md  area2.md  FACTS.md  NOTES.md
photos/CREDITS.md
TODO-운영자.md
```
`post-record.template.json`을 참고한 `performance.json`도 남긴다(질문·후보검색어·실제 발행 여부).

`TODO-운영자.md`에는 **자동화가 못 끝낸 것만** 적는다:
마지막 1구간 네이버지도 값 · 맛집 가격 A등급 · 사진 육안 확인 · 공식 링크 클릭 확인.

**커밋 전 자격증명 확인** (걸리면 커밋하지 말고 중단·보고):
```
git diff --cached | grep -E "^\+" | grep -inE \
 '(TOURAPI_KEY|X-Naver-Client-(Id|Secret))["'"'"']?[[:space:]]*[:=][[:space:]]*["'"'"']?[A-Za-z0-9%_/+.-]{12,}|serviceKey=[A-Za-z0-9%_/+.-]{12,}|Bearer[[:space:]]+[A-Za-z0-9._-]{12,}|script\.google\.com/macros'
```
★**옛 검사(`grep -inE "TOURAPI_KEY|serviceKey=|..."`)를 쓰지 마라.** 패턴 *이름*만 보기 때문에
**이 문서 자신의 검사 명령어 줄에 매번 걸린다**(2026-09-09에 실제로 두 번 걸려 커밋이 막혔다).
위 검사는 **키 뒤에 12자 이상 값이 붙은 경우만** 잡는다. 실측: 진짜 자격증명 4종 전부 탐지, 문서 문구 오탐 0건.
그래도 매칭이 나오면 **`+`로 추가된 줄인지, 값이 실제로 붙어 있는지** 두 가지를 확인하고 판단한다.
커밋 메시지: **한국어 한 줄 + `—` 뒤에 근거.**

## 9. 마무리

1. `calendar.tsv`의 해당 행 `상태`를 갱신 (`취재중`→`원고완성` 또는 `보류`). Git 커밋은 발행 증거가 아니다. `발행완료`는 실제 공개 URL·게시일·본문 대조 또는 사용자 발행 확인을 기록한 뒤에만 사용한다. 기존 지역 글로 다른 시즌 행까지 완료하지 않는다.
2. `.claude/auto/RUNLOG.md`에 한 줄 추가:
   `2026-09-09 08:00 KST · 속초 가볼만한곳 · 통과 131/150 · 52분 · 운영자확인 3건`
3. 푸시 (실패 시 2·4·8·16초 백오프 4회)
4. **`PushNotification`으로 한 줄** — 이 Routine은 상주 세션 방식이라 자동 알림이 없다.
　 ★실행 소요는 1회 약 55분이었다(취재 7분·집필 6분·평가 4분+2분·사진 10분·나머지 편집장 작업).
   예: `속초 가볼만한곳 131/150 통과 · 운영자 확인 3건`
   아무것도 안 했으면 그것도 보낸다. 예: `오늘 3건 완료, 추가 실행 없음`

---

## 하지 않는 것
- 네이버에 실제 발행 — **은희님이 한다.** 자동화는 저장소에 쌓기까지다.
- main 커밋 · PR · `posts.json` 수정
- 깨진 시즌 시나리오(4889476·5010304·4996091) 손대기
- 자격증명을 파일에 적기

---

## ★★patch 규율 — 같은 사고가 네 번 났다 (2026-09-11 기록)

> **이 항목은 원래 `CLAUDE.md`의 안티패턴 표에 들어가야 한다.**
> 지금 CLAUDE.md는 다른 작업자가 개정 중이라 건드리지 않고 여기에 적어 둔다. **정리되면 옮길 것.**

### 사고 이력 — 전부 "숫자를 고치다가 다른 줄을 깨뜨린 것"

| 언제 | 무엇 | 어떻게 깨졌나 |
|---|---|---|
| 정읍 2R | 도보 거리 정정 | 단풍터널 왕복과 접근로를 따로 세어 **거리를 이중으로 계산**(8km) |
| 정읍 3R | 코스 순서 변경 | 셔틀 설명이 **옛 순서 기준으로 남아** 종점이 어긋남 |
| 안동 2R | 코스 순서 변경 | **매표·셔틀 대기 1시간이 총소요에서 빠짐**(5:20 vs 실제 6:20) |
| **남원 3R** | **"폐장 2시간 전 도착" 권고 추가** | **같은 글의 "18시부터 무료"와 충돌.** 하절기 권고(19시)가 무료 시작(18시)보다 늦고, 동절기 권고(18시)는 무료 시작과 동일 → **"표를 언제까지 사러 가야 하나"의 답이 안 됨.** 평가 **128 → 126으로 하락** |

### ★공통 원인
**새 숫자를 넣을 때, 그 숫자와 관계된 다른 숫자를 세우지 않았다.**
남원 건은 특히 나빴다 — 내가 직접 `✅ 18:00 ~ 폐장까지 무료 개방`을 **같은 화면 여섯 줄 위에 써 놓고도**
그 아래에 충돌하는 권고를 넣었다. 읽지 않은 게 아니라 **대조하지 않은 것이다.**

### ★규칙 (patch마다 예외 없이)
1. **숫자를 넣거나 고치면, 그 숫자가 속한 '계열'을 전부 grep해 한 화면에 세운다.**
   계열 예 — 시각(폐장/매표마감/무료개방/막차/코스시각) · 금액(입장료/교통비/식비/합계) ·
   거리와 시간(구간별/총계/걷는 양) · 횟수(배차/운행 회수)
2. **세운 뒤 손으로 계산해 본다.** "범위 안에 들어가니 괜찮다"로 넘기지 않는다 —
   남원 건은 19시가 폐장 21시 '안'에 있었지만 무료 시작과 부딪혔다. **범위가 아니라 의미가 충돌한 것이다.**
3. **조건이 계절·요일·방면으로 갈리면 갈린 쪽을 전부 닫는다.**
   남원 4R에서 하절기만 고치고 동절기를 안 닫을 뻔했다.
4. **평가단에 넘기기 전에 내가 먼저 검산한다.** 평가단이 산술을 잡아 주는 건 집필 실패다.

### ★점수가 내려갈 수도 있다는 걸 전제한다
`patch = 점수 상승`이 아니다. 남원은 patch를 넣어서 **내려갔다.**
그러므로 라운드를 쓸 때 **"이 patch가 무엇을 깨뜨릴 수 있나"를 먼저 적고** 시작한다.

### ★2026-09-15 추가 — 편집장이 '개수만 세고 검산을 안 한' 사고 (목포)

목포 원고를 넘기기 전 나는 강조 개수·금지어·마크다운을 전부 세어 "고칠 게 없다"고 판단했다.
그런데 평가단이 **산술 오류**를 잡았다.

| 본문 | 값 |
|---|---|
| 55행 | `서울(용산) : KTX 평균 2시간20~23분` |
| 62행 | `09:30 시티투어를 타려면 늦어도 오전 7시 10분 전에는 KTX에 올라야 여유가 있어요` |

**7:10 + 2:20 = 9:30.** 여유가 있기는커녕 정시에 겨우 닿거나 이미 늦는다.
하차해서 승강장까지 걷는 시간을 더하면 놓친다. **시티투어는 하루 1회**라 놓치면 그날 코스가 통째로 날아간다.

**원인**: 나는 `grep -c`로 셀 수 있는 것만 검증했다. **두 줄을 나란히 세워 더해 보는 일은 grep이 못 한다.**
patch 규율 4번("평가단에 넘기기 전에 내가 먼저 검산한다")이 이미 있었는데, **개수 검증으로 그걸 대신했다고 착각한 것이다.**

**규칙**: 원고를 평가단에 넘기기 전, 개수 카운트와 **별도로** 아래를 손으로 계산한다.
1. **출발 시각 + 소요 = 도착 시각**이 본문의 다른 시각(탈것 출발·폐장·마감)과 맞는가
2. **구간 소요의 합**이 본문에 적힌 총 소요와 맞는가
3. **단가 × 인원/횟수**가 본문에 적힌 합계와 맞는가
4. **막차 시각**이 코스 종료 시각보다 뒤인가
→ 하나라도 어긋나면 평가단에 넘기기 전에 고친다. **grep으로 세지는 것과 계산해야 아는 것은 다른 검증이다.**

### ★같은 회차의 반대 사례 — 내 지시가 과했던 것
나는 "케이블카 요금을 숫자로 쓰지 마라"고 지시했다. §8에서 공식 사이트가 403이라 A등급을 못 올렸기 때문이다.
그런데 집필자는 **기자가 B등급(출처 2건 교차)으로 이미 확보한 값**을 썼고, 65세 할인만 "얘기도 있어요"로 완화했다.
FACTS를 다시 보니 매표 마감도 "양쪽 출처 일치"로 B등급이었다. **집필자의 판단이 옳았다.**
→ **A등급 실패가 곧 '쓰지 마라'가 아니다.** 지시하기 전에 FACTS에서 그 값의 등급을 먼저 확인한다.

### ★2026-09-16 추가 — **B등급을 셀 때 '출처 개수'가 아니라 '원천 개수'를 센다** (여수)

여수 불꽃축제 취재에서 기자가 이렇게 보고했다.
> `★장소가 바뀌었다 — 가장 중요한 팩트: 2026년은 처음으로 여수세계박람회장으로 이전됨`
> `— B등급(위 언론 9곳 이상 동일 보도자료 인용, 다만 전부 같은 여수시 보도자료를 재배포한 것으로 보여 완전 독립출처는 아님)`

**나는 '9곳 교차'만 보고 이걸 글의 훅으로 삼았다.** 기자가 괄호 안에 적어 둔 경고를 흘려보냈다.
평가단이 "장소와 시작 시각은 별개일 수 있다"며 **확인처를 대고** 재검토를 요구해 연도별 개최지를 세워 보니 —

| 연도 | 장소 |
|---|---|
| 2016~2023 | 이순신광장 |
| **2024** | **여수세계박람회장** |
| 2025 | 이순신광장·장군도 앞 해상 |
| 2026 | 여수세계박람회장 |

**"처음 이전"이 아니라 해마다 오가는 것이었다.** 글의 핵심 주장이 틀린 채로 나갈 뻔했다.

**규칙**
1. **보도자료 기반 기사 N건은 1건으로 친다.** 9곳이 같은 보도자료를 받아쓴 것과 9곳이 각자 취재한 것은 전혀 다르다.
   B등급(독립 2건)을 판정할 때 **원천이 몇 개인지**를 묻는다.
2. **기자가 괄호 안에 단 유보 문구를 흘려보내지 않는다.** "완전 독립출처는 아님", "같은 보도자료로 보임" 같은 문구가 붙은 값은
   **그 값을 글의 훅으로 삼기 전에 반드시 다른 각도로 한 번 더 판다.**
3. **'처음'·'유일'·'최초' 같은 단정은 연도별로 세워 보고 쓴다.** 올해 값만 보면 알 수 없는 주장이다.

### ★같은 회차 — 평가단 프롬프트에 '확인처를 대라'를 넣으면 지적의 질이 올라간다

직전 회차(순천)에서 평가단이 승인한 **내 추론**이 하마터면 사실로 굳을 뻔했다.
그래서 이번 회차부터 평가 프롬프트에 이 문구를 넣었다 —
> ★**근거 없는 지적 금지.** 본문에 없는 값을 넣으라고 요구하려면 **어디서 확인할 수 있는지**도 같이 적어라.
> 확인처를 못 대면 "확인 필요"로만 표시하라.

그랬더니 평가단이 **확인처를 댄 지적**(최근 3개년 언론·여수시 보도자료)을 했고, 그게 실제로 글의 사실오류를 잡아냈다.
→ **이 문구를 평가 프롬프트에 고정한다.** 평가단은 글만 보지 출처를 안 보지만, 확인처를 대라고 요구하면
　 "어디를 더 파야 하는지"를 짚어 준다. 그건 평가단이 잘할 수 있는 일이다.

### ★2026-09-17 추가 — **평가단은 '독자가 읽는 글'을 보지 '운영자가 붙여넣는 파일'을 안 본다** (남원 되살리기)

2사이클 8라운드를 돈 남원 원고를 다시 열었더니, **평가단이 8라운드 내내 못 본 것 2건**이 있었다.

1. **내부링크 자리표시 3개가 본문에 살아 있었다** — 형제글이 미발행인데 `[내부링크: … — 발행 후 삽입]`이 그대로.
   네이버에 붙이면 **글자로 박힌다**(마크다운 표 사고와 같은 계열).
2. **복붙 울타리(```)가 없었다** — 어디부터 어디까지가 본문인지 경계가 없어 편집자 주석까지 딸려 갈 상태였다.

평가단 잘못이 아니다. **평가단은 "이 글을 읽는 독자"를 연기하지 "이 파일을 붙여넣는 운영자"를 연기하지 않는다.**
→ **커밋 직전 편집장이 따로 보는 층위로 고정한다** (§8 전에 매번) —
　 ① 울타리 안에 본문만 있나 ② `[내부링크:`·`((`·`TODO`·`<!--` 같은 **자리표시가 울타리 안에 남았나**
　 ③ 마크다운 표 0 ④ 편집자 주석은 전부 울타리 **밖**인가

### ★같은 회차 — **되살리기 사이클은 점수를 올리러 가는 게 아니다**

남원 3차 사이클은 121→127→125→119로 끝났다. **시작(121)보다 낮게 끝났고 130에도 못 닿았다.**
그런데도 이 회차는 성과였다 — 위 2건과 **치명결함 1건**(저녁 무료 개방 권유 ↔ 대전 막차 18:40 충돌)을 잡았다.
→ **미달 원고 되살리기의 성공 기준은 점수가 아니라 '발행하면 사고 날 것'을 몇 개 걷어냈나**로 본다.
　 점수가 130에 못 닿으면 **상태는 그대로 미달로 적는다.** 치명결함을 0으로 만든 것과 130을 넘긴 것은 다른 일이다.

### ★2026-09-17 추가 — **취재 중 전제가 바뀌면 에이전트를 멈추지 말고 정정을 밀어 넣는다** (용인)

마지막 1구간이 미확보라 집필 에이전트에 **"택시를 기본으로 안내하라"**고 지시해 띄웠다.
그런데 에이전트가 도는 도중에 운영자의 네이버지도 실측이 도착했다 — **105번 버스가 있었다.**
전제가 정반대로 뒤집혔다.

→ 결과를 기다렸다가 다시 쓰면 **한 사이클을 통째로 버린다.**
→ **`SendMessage`로 정정을 밀어 넣었다.** 에이전트가 다음 tool round에서 받아 교통 섹션을 다시 쓰고,
　 첫5줄·3초요약·코스·체크리스트·한 줄 정리까지 숫자를 일관되게 맞췄다.
★정정 메시지에는 **"앞서 보낸 X 지시는 폐기한다"**를 명시한다. 새 값만 주면 옛 지시가 남는다.
★그리고 **무엇이 풀리고 무엇이 그대로 금지인지**를 같이 적는다(용인에선 '분 단위 도보시간'이 금지→허용으로 풀렸고
　 '65세 할인 금액·프로그램 날짜'는 그대로 금지였다).

### ★같은 회차 — **평가단이 동의해도, 평가단이 제안해도 근거가 아니다** (3건 연속)

용인에서 평가단 제안 3건을 그대로 쓰지 않았다. 셋 다 이유가 달랐다.

| 제안 | 왜 안 썼나 |
|---|---|
| "42분을 41분으로 고쳐라"(27+13+1=41) | **42분은 운영자가 네이버지도로 직접 조회한 실측 총합**이다. 41로 바꾸면 내가 실측값을 고치는 것이고, "환승 여유 +1분"이라 설명하는 건 근거 없는 추측이다. → 합산식처럼 읽히던 괄호를 없애고 **출처를 문장에 박았다.** 평가단이 다음 라운드에서 이 처리를 인정했다 |
| "백암터미널 매표소 전화번호를 넣어라" | 번호를 취재로 못 구했다. **지어낼 수 없다** |
| "정류장에 붙은 시간표를 같이 봐달라고 하라" | **그 정류장에 시간표가 있는지 확인하지 못했다.** 있다고 전제했다가 없으면 70대 독자가 다시 막다른 길에 선다 |

★세 번째가 특히 중요하다. 평가단은 근거로 *"정류장 표지판 사진 카드가 예정돼 있다"*고 했는데,
그건 **우리 이미지 계획**이지 현장 확인이 아니다. **아직 찍지도 않은 사진을 근거로 현장 시설을 단정할 수 없다.**

→ 대신 **확인된 값만으로 막다른 길 자체를 없앴다** —
　 `105번은 시내버스라 시외버스 창구에서는 모를 수도 있어요. 그럴 땐 기다리지 말고 택시로 가면 돼요(9.2km·약 11,900원)`.
　 ★**패치의 방향이 다르다**: 평가단은 *"물어볼 곳을 하나 더 주자"*였고, 우리는 *"물어보기가 실패해도 갈 수 있게 하자"*였다.
　 **확인 안 된 경로를 하나 더 얹는 것보다, 확인된 경로로 실패를 흡수하는 쪽이 언제나 안전하다.**

### ★같은 회차 — 사진이 **전부 Type3**일 수 있다 (area2가 미완성으로 끝나는 경우)

철원에서는 같은 지역에 Type1과 Type3가 섞여 있어 골라 쓸 수 있었다.
**용인은 등재된 7장이 전부 Type3(변경금지)였다 — 오버레이 배경이 0장이다.**

→ 이때 **글을 멈추지는 않는다.** 본문은 통과시키고, **area2를 "미완성"으로 정직히 내놓는다.**
　 ① 글자 없이 원본 그대로 쓰는 카드(분위기 컷)는 그대로 진행
　 ② 오버레이 카드는 **영문 프롬프트까지 완성해 두고** `[배경 사진 소스] ★미확보`로 표시
　 ③ **어떤 경로로 배경을 구하면 되는지**를 적는다(직접 촬영 / 공식 SNS 사용 허락 / 포토코리아 재검색)
★`image-guide.md`가 정한 *"가짜 생성 배경으로 때우지 않는다"* 를 지키는 유일한 길이다.
★그리고 **사진을 못 내려받으면 `육안 미확인`이라고 적는다.** 이 환경의 프록시는 `tong.visitkorea.or.kr`을 막는다(CONNECT 403).

### ★2026-09-18 추가 — **§2 필터는 '시즌시작'이 아니라 '발행권장' 기준이다** (내가 틀렸던 것)

09-18 1회차에 후보 추출 스크립트를 **시즌시작**으로 짰다. §2에 적힌 조건은 **발행권장**인데도 그랬다.
그 회차는 우연히 결과가 같았지만(둘 다 0건), **부수 결론이 틀렸다** —
새로 넣은 5행이 "아직 창에 못 들어온다, 엿새 더 기다려야 한다"고 적었는데
**발행권장 기준으로는 넣은 즉시 5건 전부 후보였다.** 2회차에 바로잡고 그날 원고를 썼다.

★**왜 두 값이 다른가** — `시즌시작`은 단풍·꽃이 드는 날이고, `발행권장`은 **그보다 3~4주 앞선 집필 시점**이다.
색인·랭킹에 시간이 걸리니 캘린더가 이미 그만큼 앞당겨 적어 둔 것이다.
시즌시작으로 거르면 **항상 3~4주씩 늦게 쓰게 된다.** 이 블로그에선 그게 곧 죽은 훅이다.

→ **후보 추출은 반드시 `발행권장`(0번 열)으로 한다.** 자유 텍스트라 날짜 변환이 필요하다 —
　 `N월 초`=05일 · `N월 중순`=15일 · `N월 말`=25일, 9월 이상이면 2026년 그 미만이면 2027년.
→ 그리고 **시즌종료가 오늘보다 이른 행은 따로 걸러낸다**(발행권장은 창 안인데 시즌이 이미 끝난 경우).

★교훈은 더 일반적이다. **규칙을 외워서 코드로 옮기지 말고, 옮기기 직전에 원문을 다시 읽는다.**
이건 남원 4R·용인 초안에서 잡은 "글 안의 두 숫자를 대조하지 않는 실수"와 같은 계열이다 — 대상이 원고가 아니라 절차였을 뿐이다.
