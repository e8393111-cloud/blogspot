# 키워드 실측 도구 — 네이버 월간 검색량 + 경쟁도

주제를 감으로 고르다 20편을 묻었다(→ `.claude/keyword-log.md`). 이제 **숫자로 고른다.**
이 문서는 그 측정을 **매달 다시 돌리는 방법**이다.

---

## 무엇을 재나

| 값 | 출처 | 의미 |
|---|---|---|
| `pc` / `mobile` | 네이버 **검색광고 키워드도구**(Apps Script 프록시 경유) | 월간 검색량 |
| `total` | 네이버 **검색 OpenAPI** 블로그검색 | 블로그 문서 수 = 경쟁도 |
| `경쟁률` | `total ÷ (pc + mobile + 1)` | **낮을수록 좋다** — 수요 대비 공급이 적다 |
| `category` | 검색광고가 돌려준 `relKeyword` | 조회한 키워드와 실제로 매칭됐는지 검증용 |

**황금 구간 = 검색량은 있는데(월 1,000+) 경쟁률이 낮은 키워드.**

---

## 구성 (Make · 팀 1918871)

| 리소스 | ID | 역할 |
|---|---|---|
| 데이터 구조 `여행키워드조사_구조` | 470510 | keyword / category / pc / mobile / total / ratio / measuredAt |
| 데이터스토어 `여행키워드조사` | 137112 | 측정 결과 저장 |
| 데이터스토어 `여행키워드_집계` | 137130 | 전체 결과를 TSV 한 덩어리로 (MCP 조회 100건 제한 우회) |
| 시나리오 `🧭 여행 니치조사 (키워드 실측)` | 6031917 | 측정 본체 (on-demand) |
| 시나리오 `📊 여행키워드 결과 집계` | 6032112 | 결과를 TSV로 묶어 137130에 저장 |

### 측정 시나리오 흐름
```
1 ParseJSON      키워드 배열(블루프린트에 하드코딩)
2 Iterator       한 건씩
3 HTTP           Apps Script → 검색광고 키워드도구   ★parseResponse = false
6 ParseJSON      3의 응답을 파싱                     ★이게 없으면 값이 안 잡힌다
4 HTTP           네이버 OpenAPI 블로그검색           parseResponse = true
5 AddRecord      137112에 저장 (key = 키워드, overwrite)
```

---

## ★반드시 지킬 4가지 (전부 실제로 겪은 함정)

**1. 검색광고 API는 공백이 든 키워드를 거부한다.**
`부산 여행` → `{"code":11001,"message":"hintKeywords 파라미터가 유효하지 않습니다."}`
→ 반드시 공백을 제거해 보낸다: ★Make의 `replace()`로 공백을 지우려 하지 말 것 — **동작하지 않았다.**
→ **공백 뺀 질의어를 미리 계산해 입력 배열에 `q` 필드로 함께 넣는다**(`{k:"부산 당일치기", q:"부산당일치기"}`).
(블로그검색 API는 공백을 그대로 둬도 된다. 오히려 붙이면 결과가 달라진다.)

**2. Apps Script는 `text/plain`으로 응답한다 → Make가 JSON으로 파싱하지 않는다.**
`parseResponse: true`로 두면 `data`가 문자열이라 `data.keywordList[0]`이 통째로 빈다.
→ HTTP는 `parseResponse: false`로 받고, **뒤에 `json:ParseJSON` 모듈을 하나 붙인다.**

**3. Make 매핑의 배열 인덱스는 1부터 시작한다 → 조회한 키워드는 `keywordList[1]`이다.**
`[0]`을 쓰면 **아무 값도 안 나온다**(빈 칸으로 저장돼 실패를 눈치채기 어렵다).
> 기존 시나리오 `🥇 황금키워드 자동분석 v2.1`(id 4375030)이 `[1]`을 쓰는 건 **정상**이다.

**4. 이터레이터 출력은 배열 원소의 타입에 따라 다르다.**
- 문자열 배열 → `{{2.value}}`
- **객체 배열 → `{{2.필드명}}`** (`{{2.value.필드명}}`이 아니다 — 빈 값이 나온다)
→ 이 도구는 `{k, q}` 객체 배열을 쓰므로 `{{2.k}}` · `{{2.q}}`.

---

## 다시 돌리는 법

1. **키워드 교체** — 시나리오 6031917의 모듈 1 `json` 파라미터에 있는 배열을 바꾼다
   (`mcp__Make__scenarios_get` → 블루프린트 수정 → `scenarios_update`)
2. **측정 실행** — `mcp__Make__scenarios_run` (scenarioId 6031917, responsive false)
   · 155개 기준 **약 6분 · 467 오퍼레이션**
   · `executions_get`으로 `eventType`이 `EXECUTION_END`가 될 때까지 확인
3. **집계 실행** — `scenarios_run` (scenarioId 6032112)
4. **결과 읽기** — `data-store-records_list`(dataStoreId 137130) → `measuredAt` 필드에 TSV 전체
   형식: `키워드 \t pc \t mobile \t total \t 매칭키워드`
5. **분석** — `scratchpad/analyze.py` 로 순위·황금구간 산출

### 오퍼레이션 예산
키워드 1개당 약 3회. Make Pro 월 10,000회이므로 **155개 측정 ≈ 470회(4.7%)**.
다른 시나리오(러닝 큐레이터·황금키워드 등)가 함께 도니 월 2~3회 측정이 안전선.

---

## 주의

- **자격증명은 기존 시나리오에서 재사용**한다. Apps Script URL과 네이버 API 키를 **저장소에 커밋하지 않는다.**
  필요하면 Make 블루프린트(4375030 또는 6031917)에서 읽어 쓴다.
- 검색광고 API는 검색량이 아주 적으면 `< 10`을 문자열로 준다 → 숫자 필드에 안 들어가고 비어 보인다. **빈 값 = 사실상 0에 가깝다**고 읽는다.
- 측정값은 **월간 검색량**이라 하루 이틀 차이로는 거의 안 변한다. 자주 돌릴 이유가 없다 — **월 1회면 충분하다.**

---

## ★2026-09-13 3차 측정 — 캘린더의 '가을 후반 공백'을 메웠다

**왜 돌렸나**: 09-13 파이프라인이 **두 회차 연속 "주제 후보 0건"**으로 끝났다.
필터(미발행 · 발행권장 3주 이내 · 경쟁률<5 · 뚜벅이≠불가)를 통과하는 행이 실질적으로 없었고,
원인은 지역이 아니라 **캘린더가 10월 중순~12월 초에 비어 있던 것**이었다. PIPELINE §2의 폴백대로 실측했다.

20개 측정 · 82 오퍼레이션 · 59초.

### 경쟁률 5 미만으로 통과한 7개 (검색량 = pc+mobile)
| 키워드 | 검색량 | 경쟁률 | 메모 |
|---|---|---|---|
| **군산 가볼만한곳** | 106,070 | **2.0** | ★이번 조사 검색량 1위이면서 경쟁률 최저급. 상시 소재(근대역사거리) |
| **문경 가볼만한곳** | 49,900 | **2.0** | 문경새재 단풍. 철도 없음 |
| **목포 가볼만한곳** | 67,320 | 2.8 | 목포역 KTX 종착 |
| **충주 가볼만한곳** | 53,260 | 2.9 | 충주역(중앙선) |
| **담양 가볼만한곳** | 49,910 | 3.0 | 메타세쿼이아길. 철도 없음 |
| **청송 가볼만한곳** | 15,910 | 3.5 | 주왕산. 철도 없음·검색량 작음 |
| **순천만국가정원** | 41,570 | 4.1 | ★지역명(`순천 가볼만한곳` 5.7)보다 **명소 키워드가 더 좋은 첫 사례** |

### ★탈락한 것에서 배운 것 — 명소 키워드가 지역명보다 나쁠 때가 많다
| 키워드 | 검색량 | 경쟁률 | 판정 |
|---|---|---|---|
| 죽녹원 | 7,150 | **24.7** | 담양(3.0)보다 8배 나쁘다 |
| 예산 가볼만한곳 | 25,700 | 22.8 | 지역명인데도 나쁘다 |
| 곡교천 은행나무길 | 1,930 | 14.3 | 검색량 자체가 작다 |
| 부석사 | 12,110 | 11.0 | 영주(6.5)보다 나쁘다 |
| 부여 가볼만한곳 | 52,570 | 10.5 | 검색량은 큰데 문서가 훨씬 많다 |
| 공주 가볼만한곳 | 73,890 | 8.9 | 위와 같음 |
| 주왕산 | 19,460 | 8.1 | 청송(3.5)보다 나쁘다 |

→ **유명한 단일 명소일수록 문서가 이미 쌓여 있다.** `죽녹원`·`부석사`·`주왕산`이 전부 지역명보다 나빴다.
　반대로 `순천만국가정원`은 지역명보다 좋았다. **감으로 고르지 말고 둘 다 재서 낮은 쪽을 쓴다.**

### 도구 변경 — 집계 출력을 146191로 돌렸다
집계 시나리오 6032112가 원래 결과 TSV를 **137130**에 쓰는데, 그 데이터스토어가
2026-09-08 철원 사진 조사의 `TQ_*` 레코드로 막혀 있어 `all` 레코드에 닿으려면 지우며 페이징해야 했다
(레코드 하나가 수천 자라 그 자체로 컨텍스트를 먹는다).
→ **모듈 3의 datastore를 146191(웹취재 임시)로 바꿨다.** 키는 그대로 `all`.
　146191은 매 회차 비우고 쓰는 곳이라 `limit 1`로 바로 읽힌다. 되돌리려면 모듈 3의 datastore만 137130으로 고치면 된다.

---

## ★2026-09-16 4차 측정 — 이번엔 '캘린더가 빈 것'이 아니라 '슬롯을 다 쓴 것'이었다

**왜 돌렸나**: 09-16 3회차가 **"주제 후보 0건"**으로 시작했다. 3차(09-13) 때와 증상은 같지만 원인이 다르다.
- 3차 : 캘린더의 **10월 중순~12월 초가 비어 있었다**
- 4차 : **09월 말·10월 초 슬롯을 같은 날 1·2회차(순천·여수)가 다 썼다**
→ 같은 폴백(PIPELINE §2)을 쓰되, 이번엔 **더 먼 시즌(겨울~초봄)**을 겨냥해 새 지역을 골랐다.

### 통과 4개 (경쟁률 5 미만)
| 키워드 | 검색량 | 경쟁률 | 캘린더 배치 |
|---|---:|---:|---|
| **포항 가볼만한곳** | **79,890** | **4.76** | 11월 중순 · 겨울바다(호미곶·영일대) |
| 평창 가볼만한곳 | 46,540 | 4.16 | 1월 초 · 눈(양떼목장·월정사) |
| 보령 가볼만한곳 | 32,340 | **3.45** | 2월 중순 · 봄바다(대천·무창포) |
| 정선 가볼만한곳 | 31,760 | 3.66 | 12월 초 · 눈(민둥산·아리랑시장) |

★**포항이 이번 조사의 수확이다.** 검색량 79,890으로 **1위이면서** 경쟁률이 5 미만이다.
　이 조합은 드물다 — 보통 검색량이 크면 문서도 같이 쌓여 있다.

### 탈락 7개 — '검색량만 보고 대도시로 가지 마라'가 또 확인됐다
| 키워드 | 검색량 | 경쟁률 |
|---|---:|---:|
| 대전 가볼만한곳 | 93,200 | **12.04** |
| 동해 가볼만한곳 | 29,280 | **16.42** |
| 진주 가볼만한곳 | 32,970 | 10.40 |
| 이천 가볼만한곳 | 21,840 | 9.85 |
| 천안 가볼만한곳 | 48,450 | 8.72 |
| 전주 가볼만한곳 | 71,570 | 7.75 |
| 여주 가볼만한곳 | 26,520 | 6.88 |

→ **대전은 검색량이 포항보다 큰데(93,200 vs 79,890) 경쟁률이 2.5배 나쁘다.** 문서가 112만 건 쌓여 있다.
→ 2차 조사의 `서울 44.0 · 제주 38.8 · 부산 10.5` 패턴이 중소도시급에서도 그대로 재현됐다.
→ **동해(16.42)가 뜻밖이다** — 중소도시인데 경쟁률이 대전보다 나쁘다. `동해`가 보통명사(동해 바다)라 문서가 부풀려진 것으로 보인다.
　★**지명이 보통명사와 겹치면 문서수가 과대 집계된다.** `동해`·`광주`·`남해` 같은 이름은 경쟁률을 곧이곧대로 읽으면 안 된다.

---

## ★★도구 함정 2개 — 이번에 새로 걸린 것

### 1. 집계 TSV가 조용히 잘린다
측정 20개를 돌렸는데 집계 시나리오(6032112)가 만든 `all` 레코드의 TSV는 **10개에서 끊겨 있었다.**
에러도 경고도 없다. 누적 레코드가 260줄을 넘으면서 **데이터스토어 필드 상한**에 닿은 것으로 보인다.
→ **측정 개수와 집계 줄 수를 세어 대조하지 않으면 눈치채지 못한다.**
→ 대책 : 한 번에 **10~15개씩** 끊어 측정하거나, 아래 2번 방식으로 **접두사를 나눠 따로 읽는다.**

### 2. 결과를 137130으로 빼도 못 읽는다 — 그 스토어가 막혀 있다
잘린 뒷부분을 따로 받으려고 출력을 `137130`으로 돌렸더니, **MCP 조회가 82,163자로 한도를 넘어 실패**했다.
원인은 그 스토어가 **2026-09-08 철원 사진 조사의 `TQ_*`·`IMG_*` 레코드로 가득 차 있는 것**이다.
(이 문서 3차 기록에 이미 "그 데이터스토어가 막혀 있다"고 적혀 있었는데, 같은 함정을 다시 밟았다.)
→ **대책 : 출력은 `146191`(웹취재 임시)로 보내고, 읽은 즉시 지운다.** `137130`은 쓰지 않는다.
→ 조회가 한도를 넘으면 결과가 **파일로 저장된다** — 그때는 `grep`으로 필요한 접두사만 뽑으면 컨텍스트를 아낄 수 있다.

### 나머지 10개 (접두사 `W_`로 나눠 재측정 — 위 함정 2번 대책)
| 키워드 | 검색량 | 경쟁률 | |
|---|---:|---:|---|
| **수안보온천** | 24,890 | **2.25** | ✅ **4차 조사 최저** · 충주역에서 버스 |
| 밀양 가볼만한곳 | 40,870 | 3.10 | ✅ 밀양역(경부선) |
| **대관령 양떼목장** | 41,690 | **3.16** | ✅ ★아래 참조 |
| 김천 가볼만한곳 | 17,600 | 4.32 | ✅ 검색량이 작아 우선순위 낮음 |
| 대천해수욕장 | 64,290 | 4.81 | ✅ 단 보령 지역명(3.45)이 더 낫다 |
| 온양온천 | 23,020 | 7.36 | 탈락 |
| 아침고요수목원 | 29,760 | 7.93 | 탈락 |
| 호미곶 | 14,530 | 13.18 | 탈락 |
| 허브아일랜드 | 8,160 | 16.26 | 탈락 |
| **강화도 가볼만한곳** | 6,340 | **86.45** | 탈락 — 아래 참조 |

### ★지역명 vs 명소 — 5쌍을 나란히 재 봤다
| 지역명 | 경쟁률 | 명소 | 경쟁률 | 승 |
|---|---:|---|---:|---|
| 가평 가볼만한곳 | 3.24 | 아침고요수목원 | 7.93 | 지역명 |
| 포천 가볼만한곳 | 2.77 | 허브아일랜드 | 16.26 | 지역명 |
| **평창 가볼만한곳** | 4.16 | **대관령 양떼목장** | **3.16** | ★**명소** |
| 포항 가볼만한곳 | 4.76 | 호미곶 | 13.18 | 지역명 |
| 보령 가볼만한곳 | 3.45 | 대천해수욕장 | 4.81 | 지역명 |

→ **5쌍 중 4쌍은 지역명이 낫고, 평창만 명소가 이겼다.**
→ 3차의 `순천만국가정원`(4.1 < 순천 5.7)에 이어 **명소가 이긴 두 번째 사례**다.
→ 결론은 3차와 같다 — **감으로 고르지 말고 둘 다 재서 낮은 쪽을 쓴다.** 어느 쪽이 이길지는 미리 알 수 없다.
　 캘린더의 평창 행은 주키워드를 **`대관령 양떼목장`으로 바꿔** 넣었다.

### ★`강화도` 86.45 — 경쟁률을 곧이곧대로 읽으면 안 되는 경우
검색량 6,340인데 문서가 **548,156건**이다. `동해`(16.42)와 같은 유형이지만 훨씬 극단적이다.
`강화도`는 지명이면서 **부동산·농산물·펜션 등 관광 외 문서가 대량으로 쌓이는 이름**이다.
→ **문서수가 검색량에 비해 비정상적으로 크면 그 키워드는 경쟁률 자체를 의심한다.**
　 `가볼만한곳`을 붙였는데도 문서가 50만을 넘으면, 블로그검색이 지명 단독으로 매칭하고 있을 가능성이 높다.

### 3. 레코드 하나에 값을 몰아 담으면 조회가 싸진다
필드를 `pc|mobile|total|category` 한 줄로 이어 붙여 `measuredAt`에 담으면 레코드가 작아진다.
키 접두사(`W_`·`G_`)로 회차를 구분하면 **그 회차만 골라 읽을 수 있다.**

---

## ★2026-09-17 5차 측정 — 지금까지 잰 것 중 **최저 경쟁률(0.62)**이 나왔다

**왜 돌렸나**: 캘린더 폴백이 아니다. 운영자가 드라이브(`인스타수집`)에 모아 둔 **가을 꽃축제 인스타 스냅샷 13장**에서
날짜가 살아 있는 축제를 추렸고, 그중 **용인자작나무숲 가을꽃축제(9.16~11.23, 68일)**를 고른 뒤
제목 주키워드를 정하려고 6개를 쟀다. 6개 측정 · 약 20 오퍼레이션.

| 키워드 | 검색량(pc+mo) | 문서수 | 경쟁률 | 판정 |
|---|---|---|---|---|
| **용인자작나무숲** | **17,940** | 11,093 | **0.62** | ★★역대 최저. 채택 |
| 백암순대 | 7,470 | 58,442 | 7.82 | 탈락(본문 소재로만) |
| 용인 가볼만한곳 | 33,500 | 476,038 | **14.21** | 탈락 |
| 경기도 가볼만한곳 | 47,300 | 2,566,681 | 54.26 | 탈락 |
| 자작나무숲 | 2,570 | 330,647 | **128.61** | 탈락 |
| 용인 가을꽃축제 | 35 | 12,346 | 342.94 | 탈락(검색량 자체가 없다) |

### ★배운 것 1 — "명소 키워드는 지역명보다 나쁘다"는 3차의 결론이 뒤집혔다
3차(09-13)에 `죽녹원`·`부석사`·`주왕산`이 전부 지역명보다 나빠서 **"유명한 단일 명소일수록 문서가 이미 쌓여 있다"**고 적었다.
`순천만국가정원`(4.1 < 순천 5.7)이 유일한 반례였는데, 이번에 **훨씬 극적인 두 번째 반례**가 나왔다 —
`용인자작나무숲`(0.62)이 `용인 가볼만한곳`(14.21)보다 **23배 좋다.**
→ 규칙을 고친다. **"명소 키워드가 지역명보다 나쁘다"가 아니라 "명소의 나이를 본다"**가 맞다.
　 죽녹원·부석사·주왕산은 **수십 년 쌓인 곳**이라 문서가 포화다.
　 순천만국가정원(2013 개원)·용인자작나무숲은 **수요는 붙었는데 문서가 아직 안 쌓인 곳**이다.
　 ★**신생·급성장 명소는 지역명보다 훨씬 유리하다.** 감으로 고르지 말고 **둘 다 재서 낮은 쪽을 쓴다**(이 원칙은 그대로).

### ★배운 것 2 — 고유명과 일반명의 차이가 200배다
`자작나무숲`(일반명) **128.61** vs `용인자작나무숲`(고유명) **0.62**.
일반명은 인제 원대리 등 전국의 자작나무 글이 전부 걸려 문서수가 33만 건이다.
→ **일반명사와 겹치는 이름은 반드시 지역명을 붙여 재라.** (4차의 `강화도`·`동해` 과대집계와 같은 계열)

### ★배운 것 3 — 축제 이름으로 제목을 잡으면 안 된다
`용인 가을꽃축제` 검색량이 **월 35**다. 축제 이름을 아는 사람이 거의 없다는 뜻이다.
독자는 **장소 이름으로 검색**한다(`용인자작나무숲` 17,940).
→ 축제 글이어도 **제목 주키워드는 장소명**으로 잡고, 축제는 본문 훅으로 얹는다.
　 이건 CLAUDE.md의 '날짜 의존 훅에 글 전체를 걸지 마라'와 같은 결론에 다른 경로로 도달한 것이다.

---

## ★2026-09-18 6차 측정 — 5차에서 세운 '명소의 나이' 규칙이 **같은 대상으로 검증됐다**

**왜 돌렸나**: 09-18 1회차가 **후보 0건**으로 시작했다. 원인은 3·4차와 또 달랐다 —
캘린더가 빈 것도, 슬롯을 다 쓴 것도 아니고 **가을 단풍 슬롯(10월 중순~11월 초)에 남은 게 전부 '원고완성'이나 '발행완료'**였다.
미발행 19건 중 3주 안에 시즌이 오는 게 **하나도 없었다**(가장 가까운 게 충주 44일 뒤).
→ PIPELINE §2 폴백대로 실측했다. 14개 · 약 45 오퍼레이션.

### 통과 6개 (경쟁률 5 미만)
| 키워드 | 검색량 | 문서수 | 경쟁률 |
|---|---|---|---|
| **괴산 가볼만한곳** | 18,690 | 57,219 | **3.06** |
| **순창 가볼만한곳** | 12,090 | 41,546 | **3.44** |
| **무주 가볼만한곳** | 21,780 | 75,941 | **3.49** |
| **마이산** | **41,200** | 154,789 | **3.76** ★검색량 1위 |
| **부안 가볼만한곳** | 21,710 | 88,321 | **4.07** |
| **진안 가볼만한곳** | 11,310 | 48,572 | **4.29** |

### 탈락 8개
대둔산 6.78 · 백양사 7.41 · 인제 8.85 · **원대리 자작나무숲 9.37** · 장성 10.90 ·
완주 15.67 · 산막이옛길 16.17 · 강천산 16.56

### ★★검증 — `원대리 자작나무숲` 9.37 vs `용인자작나무숲` 0.62 (**15배**)
5차에 *"명소 키워드가 나쁜 건 명소여서가 아니라 문서가 포화여서다. 신생 명소는 지역명보다 유리하다"* 고 적었다.
이번에 **같은 '자작나무숲'으로 그 규칙을 직접 시험했다.**
- **용인**(2024년 개장) : 검색량 17,940 · 문서 11,093 · **0.62**
- **원대리**(인제, 오래전부터 유명) : 검색량 2,760 · 문서 25,857 · **9.37**
→ 원대리는 **검색량이 용인의 6분의 1인데 문서는 2.3배**다. 규칙이 맞았다.
★**같은 종류의 명소라도 '언제부터 유명해졌나'가 경쟁률을 가른다.** 이게 이 도구로 찾을 수 있는 가장 큰 기회다.

### ★새로 배운 것 — 규칙에 예외가 있다: **전국구 명소는 문서가 많아도 통과한다**
`마이산`(3.76)이 `진안 가볼만한곳`(4.29)보다 좋다. 마이산은 **신생이 아닌데도** 그렇다.
문서가 154,789건으로 많지만 **검색량이 41,200으로 압도적**이라 비율이 버틴다(진안 지역명의 3.6배).
→ 규칙을 다시 정리한다. 경쟁률을 가르는 건 명소의 나이 하나가 아니라 **검색량과 문서수의 성장 속도 차이**다.
　 ①**신생 명소** — 수요가 붙었는데 문서가 아직 없다(용인 0.62 · 순천만국가정원 4.1)
　 ②**전국구 명소** — 문서는 많지만 수요가 더 크다(마이산 3.76)
　 ③**지역 안에서만 유명한 오래된 명소** — 수요는 작은데 문서만 쌓였다(산막이옛길 16.17 · 강천산 16.56 · 원대리 9.37)
★③이 가장 흔하고 가장 위험하다. **"이름은 들어봤는데 검색량은 작은 곳"**이 여기 걸린다. 감으로 고르면 여기로 간다.

### 부수 — 지역명이 명소보다 나은 경우도 그대로 있다
괴산(3.06) ≫ 산막이옛길(16.17) · 순창(3.44) ≫ 강천산(16.56) · 장성(10.90) vs 백양사(7.41)는 반대.
**둘 다 재서 낮은 쪽을 쓴다**는 원칙은 바뀌지 않는다.
